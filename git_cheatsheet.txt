git status
git init
git add . //adds all
git commit
git branch
git switch
git log
git breanchh -d [branch name] //delete a branch
git add -m //must have a comment
git push --all


vim commands :q, :w, :i, :x